query Posts($options: PageQueryOptions) {
  posts(options: $options) {
    data {
      ...PostFragment
    }
  }
}

fragment PostFragment on Post {
  id
  title
  body
  user {
    name
    email
  }
}
